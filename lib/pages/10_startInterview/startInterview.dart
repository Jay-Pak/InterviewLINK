import 'package:flutter/material.dart';

class StartInterview extends StatelessWidget {
  const StartInterview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
