import 'package:flutter/material.dart';

class OpenResume extends StatelessWidget {
  const OpenResume({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
