import 'package:flutter/material.dart';

class EndInterview extends StatelessWidget {
  const EndInterview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
