import 'package:flutter/material.dart';

class MatchingConditions extends StatelessWidget {
  const MatchingConditions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
