import 'package:flutter/material.dart';

class InterviewHistory extends StatelessWidget {
  const InterviewHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
