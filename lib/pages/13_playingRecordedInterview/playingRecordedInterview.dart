import 'package:flutter/material.dart';

class PlayingRecordedInterview extends StatelessWidget {
  const PlayingRecordedInterview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
