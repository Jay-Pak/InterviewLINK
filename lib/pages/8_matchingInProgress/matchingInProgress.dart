import 'package:flutter/material.dart';

class MatchingInProgress extends StatelessWidget {
  const MatchingInProgress({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
